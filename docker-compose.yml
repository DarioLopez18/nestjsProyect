version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: dario
      POSTGRES_PASSWORD: dariopassword
      POSTGRES_DB: nestdb
    ports:
      - 5499:5432